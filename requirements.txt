# ============================================
# JIRO AI - Dependencies
# ============================================
# Install: pip install -r requirements.txt
#
# For Windows low-end PCs, install only the
# REQUIRED section first. Add optional later.
# ============================================

# --- REQUIRED (Core) ---
httpx>=0.27.0
numpy>=1.26.0
edge-tts>=6.1.0
psutil>=5.9.0
schedule>=1.2.0

# --- AI Providers ---
# (only install what you'll use)
groq>=0.5.0

# --- GUI ---
customtkinter>=5.2.0
Pillow>=10.2.0

# --- Audio (Voice Input + Playback) ---
# NOTE: On Windows, sounddevice needs no extra install.
# On Linux, install portaudio first: sudo apt install portaudio19-dev
sounddevice>=0.4.6
SpeechRecognition>=3.10.0
pygame>=2.5.0

# --- Screen Monitoring ---
mss>=9.0.0

# --- PDF Processing ---
PyMuPDF>=1.23.0

# --- Offline LLM (optional, large) ---
# Uncomment if you want offline AI:
# llama-cpp-python>=0.2.0

# ============================================
# OPTIONAL EXTRAS
# ============================================
# pip install pystray         # System tray icon
# pip install plyer           # Desktop notifications
# pip install google-generativeai  # Gemini SDK
# pip install huggingface-hub # HuggingFace SDK
# pip install openai          # OpenAI-compatible SDK
